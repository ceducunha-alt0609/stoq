name: STOQ safety audit
on:
  push:
    paths:
      - 'stoq_audit.py'
      - '.github/workflows/stoq-audit.yml'
jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python stoq_audit.py
      - name: Commit audit report
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add stoq_audit_report.txt
          git diff --cached --quiet || git commit -m 'Generate STOQ safety audit report'
          git push
